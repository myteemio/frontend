'use client';
import { createTheme } from '@mui/material/styles';

export const teemioTheme = createTheme({
  palette: {
    primary: {
      main: '#F86655',
      contrastText: '#fff',
    },
    secondary: {
      main: '#0E67CD',
      contrastText: '#fff',
    },
    success: {
      main: '#148c40',
      contrastText: '#fff',
    },
  },
});
